/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AuthenticationHackComponent } from './authentication-hack.component';

describe('Component: AuthenticationHack', () => {
  it('should create an instance', () => {
    let component = new AuthenticationHackComponent();
    expect(component).toBeTruthy();
  });
});
